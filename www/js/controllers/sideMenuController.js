controllers.controller(AFL.PAGES.SIDE_MENU.controller, ['$scope', '$rootScope', '$utils', '$log', AFL.PAGES.SIDE_MENU.factory, function($scope, $rootScope, $utils, $log, SideMenuFactory) {
	
	$scope.logout = function() {
	}
}]);